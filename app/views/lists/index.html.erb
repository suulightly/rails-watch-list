<%= render 'navbar' %>

<div class="container">
  <h1>here are all the lists</h1>

  <% @lists.each do |list| %>
    <%= link_to list.name, list_path(list) %>
  <% end %>

  <%= link_to "Add new list", new_list_path %>
</div>
